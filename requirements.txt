# AMG OS v11.0 — Python Dependencies
# ===================================
# Pinned versions for reproducibility. Tested combination.
# Update collectively, not individually.

# Core video processing
opencv-python==4.10.0.84       # Image ops, face detection, fallback decode
# v11.1.3: PyAV (ffmpeg/libav wrapper) is the primary decode backend.
# Enables VideoToolbox hardware decode on Apple Silicon — ~3-5x faster than
# OpenCV on 4K HEVC sources, which dominates wall time on long scenes.
# OpenCV remains as a fallback if PyAV fails to import or open a file.
av==13.1.0                     # PyAV: ffmpeg wrapper for hardware-accelerated decode
ffmpeg-python==0.2.0           # ffprobe metadata wrapper

# AI client
requests==2.32.3               # HTTP for Ollama API

# Image processing
pillow==10.4.0                 # Image manipulation, contact sheet rendering, enhancement
pillow-heif==0.20.0            # HEIC/HEIF creator-provided image normalization
imagehash==4.3.1               # Perceptual hashing (dHash) for deduplication
numpy==1.26.4                  # Required by OpenCV, imagehash, ndarray ops

# Utilities
pyyaml==6.0.2                  # Config file parsing (config.yaml, studio profiles)
python-dateutil==2.9.0         # Date handling
rich==13.9.4                   # Pretty CLI output, progress bars

# Testing (dev only — comment out for production-only installs)
pytest==8.3.3
pytest-cov==5.0.0
httpx==0.28.1
